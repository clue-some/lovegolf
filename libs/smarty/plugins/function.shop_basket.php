<?php

function smarty_function_shop_basket($params, &$smarty) {
	
	//$basket = array('items' => $cart->count_contents(), 'total' => $currencies->format($cart->show_total()));
	// assign template variable
	//$smarty->assign($params['assign'], $basket);
}


?>